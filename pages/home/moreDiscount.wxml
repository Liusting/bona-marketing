<scroll-view scroll-x class="bg-white nav" scroll-with-animation scroll-left="{{scrollLeft}}">
  <view class="cu-item flex-sub {{index==TabCur?'text-green':''}}" wx:for="{{10}}" wx:key bindtap="tabSelect" data-id="{{index}}">
   <view class=" text-center">
    <text> Tab{{index}}</text>
    <!-- <view>即将开场</view> -->
   </view>
      <view class=" text-center">
    <text> Tab{{index}}</text>
    <!-- <view>即将开场</view> -->
   </view>
  </view>

</scroll-view>